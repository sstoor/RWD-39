-- SQLite
DROP TABLE item_creation;
CREATE TABLE item_creation (
    Make int(255),
    Model int(255),
    Movement varchar(255),
    Starting Bid int(255),
    Year int(255),
    Condition varchar(255),
    Description varchar(255),
    Add_Image blob(255),
    UserID int(255),
    Data varchar(255)

);SELECT UserID, Data
FROM `item_creation`;

INSERT INTO item_creation (Make,Model)
VALUES ('2', '1');